# Voix v4.1.0 configuration

# Core system settings
core:
  sanctuary: /tmp
  paths:
    - /bin
    - /sbin
    - /usr/bin
    - /usr/sbin

# Access Control List
acl:
  group:
    wheel:
      - action: permit
        options: [trust]

# Security Policies
security:
  blocklist:
    - /bin/sh
