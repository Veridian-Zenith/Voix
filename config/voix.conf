# Voix Configuration
# Ultra-simple syntax for privilege escalation

# Allow root to run any command without password
root allow nopass

# Allow wheel group members to run commands
group:wheel allow

# Allow specific users (example)
# user:username allow

# Allow specific groups (example)
# group:groupname allow

# Command restrictions (example)
# user:admin as:root allow /usr/bin/systemctl
